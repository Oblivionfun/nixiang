# 演练靶场分析记录（2026-09-12）

## 范围

本记录只覆盖授权靶场、样本完整性和演练通道的可见状态。没有启动正式测试，没有修改可执行文件，没有读取运行数据库、隐藏坐标、随机种子、内存对象或加密日志。

## 已验证事实

| 项目 | 结果 | 证据等级 |
| :--- | :--- | :--- |
| 普通样本清单 | 259 条记录全部通过大小和 SHA-256 校验 | A |
| 完整样本清单 | 272 条记录全部通过大小和 SHA-256 校验 | A |
| 主程序 | Windows amd64 PE；18,768,896 字节；SHA-256 为 `110542502401761b81f701ca1a866092e38d4ac290fa025bf18aed73ff8e9628` | A |
| 本地自动化测试 | `python3 -m pytest -q`：2 passed | A |
| 服务状态 | 可见状态返回客户端最低/最新版本均为 `1.1.0`，协议版本为 `server-protocol-v1`；状态字段显示演练摘要上传未开启 | A |
| 正式通道 | 本次没有调用、启动或点击正式测试 | A |

清单文件本身的 SHA-256：

- `simulator/artifact-manifest.json`: `5bd7cb69199c600db66d4b8b1ebb1ec8343e3d5a804fa62c389e17442f561d99`
- `simulator/full-artifact-manifest.json`: `361148917c09c37c258477a5219b0e1bc9a2f1c3c6439ed7a9d26dbd1da29ad1`

## Windows 演练观察

在 Windows amd64 官方样本中登录后，只进入了第三问和第四问的“演练测试”入口。此前两次会话都通过公开协议完成 `/enter`，各发送一次合法的 `/measure` 和 `/clear`，最后发送 `/exit`；没有读取坐标、随机种子、内存对象或日志文件内容。

| 会话 | 可见结束结果 | 公开反馈 | 证据等级 |
| :--- | :--- | :--- | :--- |
| 问题 3 演练 | 正常结束，行为日志已保存（12.3 KiB） | 共 16 个干扰源；全向 16 个，定向 0 个；测量返回方向 36.14°；清除返回 `no_target_in_range` | A |
| 问题 4 演练 | 正常结束，行为日志已保存（8.5 KiB） | 共 12 个干扰源；全向 3 个，定向 9 个；测量返回 `no_signal`；清除返回 `no_target_in_range` | A |

本次新增的一局问题 4 演练通过公开 API 完成：`/enter`、`/measure`、`/clear`、`/exit` 均返回 `accepted: true`；在 `(0,0)`、频道 `1` 的测量结果为 `no_signal`，清除结果为 `no_target_in_range`，虚拟时间从 `0` 增至 `5` 再增至 `8` 秒。该局未记录结束页源数量，不能据此推断隐藏场景或准确率。

这些是已有演练结束页和指令反馈中直接显示的样本事实，不代表固定随机分布，也不代表正式测试成绩。界面没有给出可用于宣称“准确率 100%”的评分字段；准确率及隐藏场景生成机制仍为 `unknown`。

## Windows 演练复现边界

在有权使用的 Windows 机器上，先运行清单校验，再用唯一的 `practice-YYYYMMDD-NN` 标签启动脚本。只选择第三问或第四问的演练入口，保存脱敏后的公开结果；遇到正式测试、旧会话未完成、清单不匹配或请求状态不确定时立即停止。每次结论标记为 `observed`、`inferred` 或 `unknown`，不要把演练结果写成正式成绩，也不要把未执行的准确率写成 100%。
