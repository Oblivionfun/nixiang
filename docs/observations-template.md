# 观察记录：`<session-label>`

> 复制此模板到受控本机目录，填写后先脱敏，再决定是否提交摘要。

| 字段 | 值 |
| :--- | :--- |
| session_label | `<自定义标签>` |
| sample_manifest_sha256 | `<artifact-manifest.json 的 SHA-256>` |
| mode | `practice` / `unknown` |
| question | `3` / `4` / `unknown` |
| started_at_local | `<本地时间；不要写账号信息>` |
| visible_input | `<界面/API 可见输入>` |
| visible_output | `<界面/API 可见输出>` |
| virtual_time_s | `<若有>` |
| wall_time_s | `<若有>` |
| error | `<无则填 none>` |
| evidence_path | `<本机受控目录标签>` |
| claim_level | `observed` / `inferred` / `unknown` |

## 结论

- 观察事实：
- 推断及依据：
- 尚未知事项：
- 是否需要新的授权演练：

脱敏检查：无账号、密码、robot_id、手机号、Cookie、请求 UUID、队伍信息、原始加密日志和个人绝对路径。
